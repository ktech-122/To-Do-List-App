<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>What you Got to Do</title>
</head>

<body>
    <%=messages%>
        <h1>To-Do List</h1>

        <ul>
            <% for (let todo of todos) { %>
                <li>
                    <a href="/todo/<%= todo._id %>">
                        <%= todo.title %>
                    </a>
                    <% if (todo.completed) { %>
                        - <span style="color: green;">Completed</span>
                        <% } else { %>
                            - <span style="color: red;">Incompleted</span>
                            <% } %>

                                - <form action="/todo/<%= todo._id %>/complete" method="POST">
                                    <button type="submit">Done</button>
                                </form>
                                - <form action="/todo/<%= todo._id %>/incomplete" method="POST">
                                    <button type="submit">Not Yet Done</button>
                                </form>
                </li>
                <% } %>
        </ul>

        </ul>

        <br>
        <br>
        <br>
        <form action="/logout" method="post">
            <button>Sign Out</button>
        </form>
        <a href="/todo/new">New Todo</a>
</body>

</html>